# Pinned dependencies for the Portfolio Rebalancer tool.
# The setup launcher installs these into a local .venv (never system-wide).

openpyxl==3.1.5      # CSV/Excel export of the trade plan
yfinance==1.3.0      # live price + FX snapshot from Yahoo Finance (same version as the Stock Data Dashboard)
pandas==3.0.3        # pinned to match the data stack the Dashboard fetches with successfully
numpy==2.4.6         # pinned to match the data stack the Dashboard fetches with successfully
pytest==8.3.4        # test runner used by the verify/test step
