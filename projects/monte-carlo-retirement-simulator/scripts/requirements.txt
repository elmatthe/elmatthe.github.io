# Pinned dependencies for the Monte Carlo Retirement Simulator.
# The setup launcher installs these into a local .venv (never system-wide).

numpy==2.1.3         # vectorized simulation math and percentiles
openpyxl==3.1.5      # writes the .xlsx report (summary, percentiles, chart)
matplotlib==3.9.2    # renders the fan chart embedded in the workbook
pytest==8.3.4        # test runner used by the verify/test step
